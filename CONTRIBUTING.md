# المساهمة

## قبل أي تعديل

```bash
git status          # من يعمل على ماذا؟ لا تستخدم add -A عند العمل المشترك
git pull --rebase   # حدّث شجرتك أولاً
```

## أثناء العمل

- فرع لكل ميزة (`feat/…`)، commits صغيرة برسائل واضحة (عربي أو إنجليزي بصيغة الأمر).
- **ممنوع**: كسر `correctAnswer` قبل الكشف، الثقة بساعات العميل، الإيموجي كأيقونات (الحارس سيمنعك)، الألوان الثابتة بدل التوكنز، أسئلة المشاهير، الحقائق غير المتحقق منها.
- الأسئلة الجديدة: طبّق قواعد `docs/03-questions.md` واترك `dataset.test.ts` أخضر.

## قبل الرفع (البوابة)

```bash
npm run typecheck && npm run test && npm run lint && npm run build
```

ثم push وPR صغير مع وصف وما تم التحقق منه. الـCI يعيد الفحص تلقائياً.

## العمل المشترك (وكيل + بشر)

- ملفاتك وملفات الآخرين: `git add <paths>` فقط — أبداً `add -A` عميانياً.
- تعارض؟ الأحدث منطقياً يفوز بعد الفهم، لا بالقوة (`push --force` ممنوع).
